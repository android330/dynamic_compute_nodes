[Unit]
Description=The startup script for the dynamic compute nodes
After=network.target

[Service]
Type=oneshot
ExecStart=/usr/bin/env bash /usr/local/bin/start_script.sh

[Install]
WantedBy=multi-user.target
